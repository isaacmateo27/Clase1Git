Inicializa el seguimiento de la carpeta
git init

Agregar archivos para su control 
git add .\adios.txt  // agrega solo referido

git add . agrega todos los archivos

u sin seguimiento
a archivo añadido 
m archivo modificado 

commit es un cambio con comentario, agregar comentario a la etapa
git commit -m "comentario"

Agregar y comentar los cambios

git commit -am "Modiffique el archivo 1"

Crear una rama

git branch rama1

como ver la ramas creadas: 
git branch 

cambiar de rama:
git checkout rama1

Fusionar Ramas:
git merge nombre de la rama que quiero unir